export const convertToString = (digit: number): string => {
  return `${digit}`;
};

export function numberToStringTwo(num: number): string {
  return String(num);
}

export function numberToStringThree(num: Number) {
  return num.toString();
}

export const numberToStringFour = String;
